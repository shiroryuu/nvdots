* My Nvim Wiki
** TODOs
   - ( ) Fix `snacks.nvim` lsp_definitions picker.
   -- NOTE: use `telescope`.
   - ( ) Finalize btw `vimwiki` and `neorg`
   - ( ) Improve `neorg` docs.
   - ( ) Set foldlevel to `2` or `4` for plaintext buffers like `md`,`neorg`.
   - ( ) `foldmethod = expr` vs `foldmethod = indent`
   - ( ) Investigate why `render-markdown.nvim` is crashing when `vimwiki` is loaded.
   - (-) NVF Configuration
   -- (x) setup colorscheme to kanagawa-Dragon
   -- ( ) migrate config
   --- ( ) LSPs
   --- ( ) linters
   --- ( ) Icons
   --- ( ) Custom utils
   -- (-) migrate plugins
   --- (x) Mini.nvim
   --- (x) oil.nvim
   --- (-) neorg
   --- (x) treesitter
   --- ( ) Markdown
   ---- ( ) Markdown-render
   ---- ( ) Markdown-preview
   --- ( ) harpoon
   --- (x) undotree
   --- ( ) nvim-colorizer
   --- ( ) todo-comments.nvim
   --- ( ) image.nvim
   --- ( ) diagram.nvim
   --- ( ) zen-mode.nvim
   -- ( ) New Plugins
   --- ( ) Mini.statusline
   --- ( ) Mini.input
   -- (x) migrate language defaults
   -- (x) accessing plugin functions using nvf
   --- for ex. running `require("snacks.statuscolumn").get()` in `statuscolumn` when snacks is loaded in nix.
   --- (x) set `vim.options.formatexpr` to `conform-nvim.formatexpr()`.
   --- -( ) set `vim.options.statuscolumn` to `snacks.statuscolumn.get()`.-
   --- Note: Moved to `mini.statuscolumn`.
   -- (x) Autocmds
   --- ( ) Macro recording notification
   -- ( ) Custom Module
   --- ( ) For `Utils/ui`
   -- ( ) Add Shortcut for minitrailspace trim
   --- REFER: `https://github.com/rafi/vim-config`
   --- `{ '<leader>cw', '<cmd>lua MiniTrailspace.trim()<CR>', desc = 'Erase Whitespace' },`

** Fix
   - ( ) keybinds for cmp
